# 03 — ORB-PU synthesizability
Parallel PU-bagging abstaining classifier; CGNF-matched comparison; Optuna-tuned (ORB+Magpie+XGB).

- `src/apu_synthesizability/` the package
- `results/` Optuna leaderboard, metrics, generated-structure scores
- `figures/`

The 8 trained checkpoints (.joblib) are on HuggingFace: SheikhAhnaf/apu-synthesizability-checkpoints (see ../docs/EXTERNAL.md). Figures/leaderboard regenerate from in-tree metrics CSVs.
